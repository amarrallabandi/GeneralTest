name: generatefileout

on:
    workflow_dispatch:

        
jobs:
    generate:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: generate file
              working-directory: FirstFolder
              run: |
                echo "Hello" > temp.txt



        
          

        
        